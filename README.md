### Directories
* lib - npm package
* tests - integration tests on lib folder

### Deploy new package

```terminal
cd lib
yarn build
cd dist
npm publish
```

### Test

```terminal
cd tests
yarn test
```
